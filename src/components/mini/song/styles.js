import styled from 'styled-components'

export const SharedStyle = styled.div`
    font-size: 0.9em;
    color: white;
    max-width: 600px;
    overflow: hidden;
    white-space: nowrap;
    text-overflow: ellipsis;
`
