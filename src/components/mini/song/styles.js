import styled from 'styled-components'

export const SharedStyle = styled.div`
    font-size: 1.2em;
    color: white;
    max-width: 600px;
    overflow: hidden;
    white-space: nowrap;
    text-overflow: ellipsis;
`
